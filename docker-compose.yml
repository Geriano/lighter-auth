name: lighter
version: '3.3'
services:
  lighter-auth-release:
    container_name: lighter-auth-release
    build:
      dockerfile: ./Dockerfile
      tags: 
        - lighter-auth:release
      args:
        DATABASE_URL: ${DATABASE_URL}
        PORT: ${PORT}
    expose:
      - 6789
    ports:
      - 6789:6789
